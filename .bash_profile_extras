# Shared login-shell extras loaded by install.sh

if [ -d "$HOME/bin" ]; then
    PATH="$PATH:$HOME/bin"
fi

export PATH

if [ -f "$HOME/.bash_profile_local" ]; then
    . "$HOME/.bash_profile_local"
fi
