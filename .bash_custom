# Personal overrides copied into ~/.bash_custom on first install.
# Keep machine-specific aliases here.

alias ..code='cd ~/code'
alias foundation='ssh foundation'
alias darrelld='ssh darrelld'

# must first install Heroku `curl https://cli-assets.heroku.com/install.sh | sh`
alias fire='heroku ps:exec -a ddd-quotes'
